<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Dev: HTTP to api-gateway on LAN or emulator (10.0.2.2). Use HTTPS in production. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
